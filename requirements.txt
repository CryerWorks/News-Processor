# Core Flask dependencies
Flask==2.3.3
Flask-SocketIO==5.3.6
python-dotenv==1.0.0
Werkzeug==2.3.7

# Production server dependencies (stable versions with wheels)
gunicorn==21.2.0

# Data processing dependencies (Python 3.13 compatible with wheels)
pandas==2.2.3
numpy==2.1.0
scikit-learn==1.5.2
openpyxl==3.1.5

# AI and document processing (compatible versions)
openai==1.55.3
httpx==0.27.2
python-docx==1.1.2

# Additional stability packages
setuptools>=65.0.0
wheel>=0.40.0
